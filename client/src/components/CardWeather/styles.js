import { createStyles } from "@material-ui/core";

export const color = "#F9CE94";

export const styles = createStyles({
  card: {
    width: "100%",
    maxWidth: 400,
    background:
      "linear-gradient(180deg, rgba(252,135,115,1) 0%, rgba(254,111,103,1) 100%)"
  },

  text: {
    color: color
  }
});
